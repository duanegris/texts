#include <stdio.h>
#include <unistd.h>
#include <sys/types.h>
#include <sys/stat.h>
#include <fcntl.h>
#include <ctype.h>


int main (char * str)
{
  int nb1;
  int fich;
  if (sizeof(*str) == 0)
    printf("il faut indiquer un fichier");
  else
    {
      fich = open(str,O_RDONLY);
      printf("tapper un nombre entre 0 et 2e24\n");
      scanf("%d",&nb1);
      if(nb1>2e24) printf("valeur trop grande\n");
      read(0,fich,nb1+1);
    }
  return 0;
}
