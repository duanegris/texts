
To generate the HTML document, use either
 
*  db2html -i extra-xsl.xsl php.xml
   with openjade  

or 

* xmlto -m extra-xsl.xsl html php.xml
  with xmlto (based on xsltproc)

files used:
----------
* extra-xsl.xsl is an XSL fragment that tells docbook-xml.xsl to include our
  CSS stylesheet
* duaneg-docbook.css : our CSS stylesheet
