/*
 * This is sample code generated by rpcgen.
 * These are only templates and you can use them
 * as a guideline for developing your own functions.
 */

#include "myservice.h"


void
mycomputation_1( char* host )
{
	CLIENT *clnt;
	void  *result_1;
	char*  mycomputation_null_1_arg;
	response  *result_2;
	data  mycomputation_addition_1_arg;
	clnt = clnt_create(host, MYCOMPUTATION, VERSION_ONE, "udp");
	if (clnt == NULL) {
		clnt_pcreateerror(host);
		exit(1);
	}
	/*
	result_1 = mycomputation_null_1((void*)&mycomputation_null_1_arg, clnt);
	if (result_1 == NULL) {
		clnt_perror(clnt, "call failed:");
	}
	*/

	mycomputation_addition_1_arg.arg1 = 33;
	mycomputation_addition_1_arg.arg2 = 6;

	result_2 = mycomputation_addition_1(&mycomputation_addition_1_arg, clnt);
	if (result_2 == NULL) {
		clnt_perror(clnt, "call failed:");
	}

      printf("La somme est : %d\n", result_2->somme);



	clnt_destroy( clnt );
}


main( int argc, char* argv[] )
{
	char *host;

	if(argc < 2) {
		printf("usage: %s server_host\n", argv[0]);
		exit(1);
	}
	host = argv[1];
	mycomputation_1( host );
}
